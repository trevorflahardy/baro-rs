# Baro UI System (deprecated)

This file used to contain the primary UI documentation.

The current, maintained docs live in module-level Rust docs:
- `src/ui/mod.rs` for an overview + quickstart
- `src/ui/layouts/container.rs` for `Container` / flex-like layout behavior
- `src/ui/styling/*` for styling primitives

If you're reading this on GitHub, use `cargo doc --open`.
