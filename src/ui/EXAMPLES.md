# UI Examples (deprecated)

These examples have been migrated into Rustdoc examples.

Start here:
- `src/ui/mod.rs` — overview + quickstart
- `src/ui/layouts/container.rs` — layout examples (`SpaceEvenly`, `Grow`, etc.)

Build docs locally:

```bash
cargo doc --open
```
